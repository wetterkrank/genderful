Playing with machine learning with the use of Tensorflow and Keras
Task at hand: predicting German noun genders using a neural network.

Note: if your Python's default encoding is not UTF-8, make sure to set this environment variable:
PYTHONIOENCODING=utf-8
